# Copy to local.properties (gitignored).
sdk.dir=/path/to/Android/sdk

# Optional, debug builds only: preload an OpenAI-compatible endpoint so you don't
# have to type it into Settings. Release builds ignore these.
# tokenflow.baseUrl=https://api.openai.com
# tokenflow.apiKey=sk-...
